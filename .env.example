POSTGRES_DB=athle
POSTGRES_USER=mypacer
POSTGRES_PASSWORD=change-me
POSTGRES_DEFAULT_DB=postgres
POSTGRES_PORT=5432

# Published ports on the host (used by nginx host reverse-proxy)
API_PORT=8000
WEB_PORT=8080

# Image tags (override to pin a specific release)
SCRAPER_IMAGE_TAG=latest-prod
API_IMAGE_TAG=latest-prod
WEB_IMAGE_TAG=latest-prod
